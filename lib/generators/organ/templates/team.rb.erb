class CreateTeam < ActiveRecord::Migration<%= migration_version %>
  def change
    create_table :teams<%= table_options %> do |t|
      t.string :name

      t.belongs_to :owner
      t.timestamps
    end
  end
end